spring.application.name=MediStock

# PostgreSQL. Environment variables can override these defaults without changing code.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/medistock}
spring.datasource.username=postgres
spring.datasource.password=rahul@K9182
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.open-in-view=false

server.port=${SERVER_PORT:8082}
